setTimeout(function() {
  // If bootstrap stylesheet failed to load, use local fallback
  if ($('body').css('color') != 'rgb(51, 51, 51)') {
    $('head').append('<link rel="stylesheet" href="' + d.posts.assets_url + '/bootstrap/css/bootstrap.min.css"/>');
  }
}, 1000);
