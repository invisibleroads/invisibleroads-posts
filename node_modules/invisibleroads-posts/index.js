setTimeout(function() {
  // If bootstrap stylesheet failed to load, use local fallback
  if ($('body').css('color') != 'rgb(51, 51, 51)') {
    $('head').append('<link rel="stylesheet" href="X/bootstrap/css/bootstrap.min.css">'.replace('X', d.invisibleroads_posts.assets_url));
  }
}, 1000);
